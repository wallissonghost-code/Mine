const KEY='mine-sound';export function audio(){let ctx=null,on=localStorage.getItem(KEY)!=='0';function get(){if(!on)return null;ctx??=new (window.AudioContext||window.webkitAudioContext)();if(ctx.state==='suspended')ctx.resume();return ctx}function tone(freq=300,d=.06,type='sine',gain=.045,end=freq*.55){const c=get();if(!c)return;const t=c.currentTime,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);o.frequency.exponentialRampToValueAtTime(Math.max(30,end),t+d);g.gain.setValueAtTime(gain,t);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(g).connect(c.destination);o.start(t);o.stop(t+d)}function play(k){if(k==='hit')tone(420,.045,'triangle',.025,250);if(k==='break')tone(700,.09,'square',.035,260);if(k==='wall')tone(220,.035,'sine',.018,180);if(k==='spawn'){tone(330,.07,'sine',.04,650);setTimeout(()=>tone(660,.08,'sine',.03,900),45)}if(k==='level'){tone(440,.12,'triangle',.05,880);setTimeout(()=>tone(660,.14,'triangle',.05,1100),100)}}function toggle(){on=!on;localStorage.setItem(KEY,on?'1':'0');if(on)play('spawn');return on}addEventListener('pointerdown',()=>get(),{once:true});return{play,toggle,enabled:()=>on}}